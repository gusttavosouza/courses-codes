console.log(soma(3, 4))



//function declaration
function soma(a, y) {
    return a + y
}

//function expression
const sub = function (a, b) {
    return a - b
}

//named function explression //uso bem reduzido
const mult = function mult(x, y) {
    return x * y
}

//function declaration pode ser chamada antes de ser declarada. já a function expression só pode ser chamada depois de declarada

